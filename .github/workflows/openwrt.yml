name: 构建 x86_64 中文 IPv6 固件

on:
  workflow_dispatch:          # 手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 安装编译依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
          python3-dev python3-pip python3-setuptools rsync unzip zlib1g-dev file wget
          # ↑ 唯一改动：python3-distutils → python3-setuptools

      - name: 克隆 OpenWrt 源码
        run: |
          git clone --depth 1 https://github.com/openwrt/openwrt.git openwrt

      - name: 更新 feeds
        working-directory: openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 生成定制化配置
        working-directory: openwrt
        run: |
          cat > .config <<EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_ROOTFS_EXT4FS=y
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_GRUB_IMAGES=y
          CONFIG_GRUB_EFI_IMAGES=y
          CONFIG_IPV6=y
          CONFIG_KERNEL_IPV6=y
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_wget=y
          CONFIG_PACKAGE_openssh-sftp-server=y
          EOF

          make defconfig

      - name: 预设中文界面为默认语言
        working-directory: openwrt
        run: |
          mkdir -p files/etc/config
          cat > files/etc/config/luci <<EOF
          config core 'main'
              option lang 'zh_cn'
          EOF
          mkdir -p files/etc
          echo "Asia/Shanghai" > files/etc/TZ
          # 确保 FILES 选项被识别
          echo "CONFIG_FILES=\"files\"" >> .config

      - name: 开始编译
        working-directory: openwrt
        run: |
          make -j$(nproc) V=s

      - name: 收集固件
        if: success()
        run: |
          mkdir -p firmware
          cp openwrt/bin/targets/x86/64/*.img.gz firmware/ 2>/dev/null || true
          cp openwrt/bin/targets/x86/64/*.vmdk firmware/ 2>/dev/null || true
          cp openwrt/bin/targets/x86/64/*.iso firmware/ 2>/dev/null || true
          echo "BUILD_DATE=$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-x86_64-zh_CN-IPv6-${{ env.BUILD_DATE }}
          path: firmware/
